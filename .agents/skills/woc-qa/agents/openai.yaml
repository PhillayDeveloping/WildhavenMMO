interface:
  display_name: "Wildhaven QA"
  short_description: "Coordinate scoped tests and specialist review"
  default_prompt: "Use $woc-qa to validate the current Wildhaven changes and report whether they are ready."
policy:
  allow_implicit_invocation: true
